# わにわにパニック 詳細設計書 v2

---

## 0. 確定事項（検討中.md の回答）

| 項目 | 決定値 |
|------|--------|
| ゲーム時間 | 60 秒 |
| 怒りフェーズ長さ | **固定 6 秒**（55 秒〜61 秒でゲーム終了） |
| 吻の長さ比率 | 全体 150px 中、吻先〜目の中心まで **50px**（33%） |
| 脚の形 | 丸みのある三角形、前脚 y≈90、後脚 y≈120（SVG 座標） |
| 噛みアニメーション | 下顎を 20px 開ける → 0.35s 開く → 0.8s 保持 → 0.25s 閉じる |
| 茂みビジュアル | **SVG**（BushSvg コンポーネント）で実装 |
| 叩かれた時の声 | 「いて！」（上に浮かんで消える） |
| 噛む時の声 | 「ガブ！」（赤字でドンと出て消える） |

---

## 1. ファイル構成

```
src/
├── main.tsx
├── index.css             # グローバルリセット・フォント・背景
├── App.tsx               # ルートコンポーネント
├── App.css
├── HUD.tsx               # スコア・残り時間・噛まれ数表示
├── HUD.css
├── Board.tsx             # 5列を並べるコンテナ
├── Board.css
├── Lane.tsx              # 1列分（茂み＋ワニ＋キーラベル）
├── Lane.css
├── GatorSvg.tsx          # ワニSVG（状態・種別で変化）
├── GatorSvg.css          # ワニのCSS アニメーション
├── BushSvg.tsx           # 茂みSVG
├── AngerBanner.tsx       # 「もう、怒ったぞー！」
├── AngerBanner.css
├── StartOverlay.tsx      # タイトル画面
├── GameOverOverlay.tsx   # ゲームオーバー画面
├── Overlay.css           # 両オーバーレイ共通スタイル
├── useGame.ts            # ゲームループ・ロジック全体
├── useSpawn.ts           # スポーンスケジューラ
├── types.ts              # 型定義
└── constants.ts          # 全定数
```

---

## 2. 型定義（types.ts）

```ts
export type GatorKind = "slow-full" | "quick-full" | "peek" | "biter";

/** ワニ1体の状態機械 */
export type GatorPhase =
  | "hidden"      // 非表示（アクティブでない）
  | "rising"      // 出現中（下から上がってくる）
  | "up"          // 完全に出た状態で待機中
  | "biting"      // 噛み動作中（biter のみ）
  | "retreating"  // 引っ込み中
  | "whacked";    // 叩かれた（ヒットフラッシュ中）

export type GatorState = {
  id: string;              // ユニーク ID（`${laneId}-${Date.now()}`）
  laneId: number;          // 0〜4
  kind: GatorKind;
  gatorPhase: GatorPhase;
  riseProgress: number;    // 0.0〜1.0（0=完全隠れ、1=完全出現）
  riseDuration: number;    // ms（出現にかかる時間）
  retreatDuration: number; // ms（引っ込みにかかる時間）
  upDuration: number;      // ms（出た状態で待機する時間）
  upElapsed: number;       // ms（up 状態の経過時間）
  biteElapsed: number;     // ms（biting 状態の経過時間）
  whackElapsed: number;    // ms（whacked 状態の経過時間）
};

export type GamePhase = "idle" | "early" | "mid" | "late" | "anger" | "gameover";

export type GameState = {
  gamePhase: GamePhase;
  timeLeft: number;        // 残り秒数（表示用）
  elapsed: number;         // 経過秒数
  score: number;
  biteCount: number;
  gators: GatorState[];    // アクティブなワニ全体（最大 5 体）
  angerBannerVisible: boolean;
};
```

---

## 3. 定数（constants.ts）

```ts
export const LANE_COUNT = 5;
export const GAME_DURATION = 60;    // 秒
export const ANGER_START_AT = 55;   // 経過秒：怒りフェーズ開始
export const ANGER_DURATION = 6000; // ms：怒りフェーズの長さ
export const ANGER_BANNER_DURATION = 2000; // ms：バナー表示時間

export const PHASE_EARLY_END = 20;  // 経過秒
export const PHASE_MID_END   = 40;
export const PHASE_LATE_END  = 55;

export const KEY_MAP: Record<string, number> = {
  a: 0, s: 1, d: 2, f: 3, g: 4,
};

/** ワニSVG の高さ（px）—レイアウト計算に使用 */
export const GATOR_HEIGHT = 150;
/** ワニSVG の幅（px） */
export const GATOR_WIDTH  = 70;

/**
 * 種別ごとの設定
 * riseRatio: 最大露出量（1.0 = 全身、0.35 = 頭だけ）
 * riseDuration, retreatDuration: [min, max] ms
 * upDuration: [min, max] ms（up 状態の滞在時間）
 * bites: true なら up 終了後に biting へ遷移
 */
export const GATOR_CONFIG = {
  "slow-full": {
    riseRatio: 1.0,
    riseDuration:    [1400, 2000] as [number, number],
    retreatDuration: [1200, 1800] as [number, number],
    upDuration:      [800,  1500] as [number, number],
    bites: false,
  },
  "quick-full": {
    riseRatio: 1.0,
    riseDuration:    [400, 700]   as [number, number],
    retreatDuration: [300, 600]   as [number, number],
    upDuration:      [300, 600]   as [number, number],
    bites: false,
  },
  "peek": {
    riseRatio: 0.35,
    riseDuration:    [600, 900]   as [number, number],
    retreatDuration: [500, 800]   as [number, number],
    upDuration:      [500, 900]   as [number, number],
    bites: false,
  },
  "biter": {
    riseRatio: 1.0,
    riseDuration:    [1500, 2200] as [number, number],
    retreatDuration: [1000, 1400] as [number, number],
    upDuration:      [600,  1000] as [number, number], // up 後に biting へ
    bites: true,
  },
} as const satisfies Record<GatorKind, {
  riseRatio: number;
  riseDuration: [number, number];
  retreatDuration: [number, number];
  upDuration: [number, number];
  bites: boolean;
}>;

/** 噛みアニメーションの合計時間（ms）*/
export const BITE_ANIMATION_DURATION = 1400; // 0.35 open + 0.8 hold + 0.25 close
/** 叩かれフラッシュの時間（ms）*/
export const WHACK_DURATION = 400;

export const SPAWN_TABLE: Record<GamePhase, Partial<Record<GatorKind, number>>> = {
  idle:     {},
  early:    { "slow-full": 1.0 },
  mid:      { "slow-full": 0.4, "quick-full": 0.3, "peek": 0.2, "biter": 0.1 },
  late:     { "slow-full": 0.15, "quick-full": 0.3, "peek": 0.2, "biter": 0.35 },
  anger:    { "quick-full": 0.4, "biter": 0.6 },
  gameover: {},
};

/** フェーズごとの同時出現最大数 */
export const MAX_CONCURRENT: Record<GamePhase, number> = {
  idle: 0, early: 1, mid: 3, late: 4, anger: 5, gameover: 0,
};

/** 次スポーンまでの待ち時間 [min, max] ms */
export const SPAWN_INTERVAL: Record<GamePhase, [number, number]> = {
  idle:     [0,    0   ],
  early:    [900,  1500],
  mid:      [500,  1000],
  late:     [300,  700 ],
  anger:    [80,   200 ],
  gameover: [0,    0   ],
};
```

---

## 4. ゲームロジック（useGame.ts）

### 4-1. 初期状態

```ts
const initialState: GameState = {
  gamePhase: "idle",
  timeLeft: GAME_DURATION,
  elapsed: 0,
  score: 0,
  biteCount: 0,
  gators: [],
  angerBannerVisible: false,
};
```

### 4-2. フェーズ計算

```ts
function calcGamePhase(elapsed: number, angerActive: boolean): GamePhase {
  if (angerActive)          return "anger";
  if (elapsed < PHASE_EARLY_END) return "early";
  if (elapsed < PHASE_MID_END)   return "mid";
  return "late";
}
```

### 4-3. メインループ（requestAnimationFrame）

`useGame` は `useEffect` でひとつの `rAF` ループを回す。  
ループ内で `dt`（前フレームからの経過 ms）を計算し、すべての `GatorState` を更新する。

```
rAF コールバック(timestamp):
  dt = timestamp - prevTimestamp（最大 100ms にクランプ）
  prevTimestamp = timestamp

  setGameState(prev => {
    if (prev.gamePhase === "idle" || prev.gamePhase === "gameover") return prev;

    const updatedGators = prev.gators.map(g => tickGator(g, dt, prev));
    const completed = updatedGators.filter(g => g.gatorPhase === "hidden");
    const active    = updatedGators.filter(g => g.gatorPhase !== "hidden");
    
    // hidden になったワニを biteCount に反映済みなので除去
    return { ...prev, gators: active };
  });
```

### 4-4. tickGator（1フレーム分のワニ状態更新）

```ts
function tickGator(g: GatorState, dt: number, state: GameState): GatorState {
  const cfg = GATOR_CONFIG[g.kind];

  switch (g.gatorPhase) {
    case "rising": {
      const newProgress = g.riseProgress + dt / g.riseDuration;
      if (newProgress >= 1.0) {
        return { ...g, riseProgress: 1.0, gatorPhase: "up", upElapsed: 0 };
      }
      return { ...g, riseProgress: newProgress };
    }

    case "up": {
      const newUpElapsed = g.upElapsed + dt;
      if (newUpElapsed >= g.upDuration) {
        if (cfg.bites) {
          return { ...g, upElapsed: newUpElapsed, gatorPhase: "biting", biteElapsed: 0 };
        }
        return { ...g, gatorPhase: "retreating" };
      }
      return { ...g, upElapsed: newUpElapsed };
    }

    case "biting": {
      const newBiteElapsed = g.biteElapsed + dt;
      if (newBiteElapsed >= BITE_ANIMATION_DURATION) {
        // 噛み完了 → biteCount は setGameState の外で処理（後述）
        return { ...g, biteElapsed: newBiteElapsed, gatorPhase: "retreating", _didBite: true };
        // ※ _didBite フラグで親ロジックが biteCount++ する
      }
      return { ...g, biteElapsed: newBiteElapsed };
    }

    case "retreating": {
      const newProgress = g.riseProgress - dt / g.retreatDuration;
      if (newProgress <= 0) {
        return { ...g, riseProgress: 0, gatorPhase: "hidden" };
      }
      return { ...g, riseProgress: newProgress };
    }

    case "whacked": {
      const newElapsed = g.whackElapsed + dt;
      if (newElapsed >= WHACK_DURATION) {
        return { ...g, whackElapsed: newElapsed, gatorPhase: "hidden" };
      }
      return { ...g, whackElapsed: newElapsed };
    }

    default:
      return g;
  }
}
```

> **注意**: `_didBite` は一時的なフラグ。`setGameState` のレデューサ内で `_didBite` が true のワニを検出したら `biteCount++` を加算し、フラグを削除してから `active` リストに含める（retreating 状態のまま引っ込む）。

### 4-5. カウントダウン

`useEffect` で `setInterval(1000)` を走らせ、`elapsed` を +1、`timeLeft` を -1 する。  
`elapsed >= ANGER_START_AT` になったら怒りフェーズ突入シーケンス：

```
1. 全アクティブワニを即時 gatorPhase: "retreating" にする
2. gamePhase: "anger"、angerBannerVisible: true にする
3. setTimeout(ANGER_BANNER_DURATION) → angerBannerVisible: false
4. setTimeout(ANGER_DURATION) → gamePhase: "gameover"
```

### 4-6. スポーンスケジューラ（useSpawn.ts に切り出し）

```ts
// useSpawn.ts が公開するシグネチャ
function useSpawn(
  gamePhase: GamePhase,
  activeGatorCount: number,
  occupiedLanes: Set<number>,
  onSpawn: (gator: GatorState) => void,
): void
```

内部ロジック:
```
scheduleNext():
  if gamePhase is "idle" | "gameover" | "anger-waiting-banner": return
  wait = randomBetween(SPAWN_INTERVAL[gamePhase])
  setTimeout(wait, () => {
    if activeGatorCount >= MAX_CONCURRENT[gamePhase]: scheduleNext(); return
    lane = pick random from [0..4] not in occupiedLanes
    if no lane available: scheduleNext(); return
    kind = pickKind(SPAWN_TABLE[gamePhase])
    cfg  = GATOR_CONFIG[kind]
    gator = {
      id: `${lane}-${Date.now()}`,
      laneId: lane,
      kind,
      gatorPhase: "rising",
      riseProgress: 0,
      riseDuration:    randomBetween(...cfg.riseDuration),
      retreatDuration: randomBetween(...cfg.retreatDuration),
      upDuration:      randomBetween(...cfg.upDuration),
      upElapsed: 0, biteElapsed: 0, whackElapsed: 0,
    }
    onSpawn(gator)
    scheduleNext()
  })
```

`pickKind` は重み付きランダム選択：
```ts
function pickKind(table: Partial<Record<GatorKind, number>>): GatorKind {
  const entries = Object.entries(table) as [GatorKind, number][];
  const r = Math.random();
  let acc = 0;
  for (const [kind, prob] of entries) {
    acc += prob;
    if (r < acc) return kind;
  }
  return entries[entries.length - 1][0];
}
```

### 4-7. whack（叩き処理）

```ts
function whack(laneId: number) {
  setGameState(prev => {
    if (prev.gamePhase !== "early" && prev.gamePhase !== "mid" &&
        prev.gamePhase !== "late" && prev.gamePhase !== "anger") return prev;
    
    const target = prev.gators.find(g =>
      g.laneId === laneId &&
      (g.gatorPhase === "rising" || g.gatorPhase === "up" || g.gatorPhase === "biting")
    );
    if (!target) return prev;

    const bonus = prev.gamePhase === "anger" ? 2 : 1;
    return {
      ...prev,
      score: prev.score + bonus,
      gators: prev.gators.map(g =>
        g.id === target.id
          ? { ...g, gatorPhase: "whacked", whackElapsed: 0 }
          : g
      ),
    };
  });
}
```

キーボード操作:
```ts
useEffect(() => {
  const handler = (e: KeyboardEvent) => {
    if (e.repeat) return;
    const laneId = KEY_MAP[e.key.toLowerCase()];
    if (laneId !== undefined) whack(laneId);
  };
  window.addEventListener("keydown", handler);
  return () => window.removeEventListener("keydown", handler);
}, [whack]);
```

---

## 5. レイアウト仕様

### 5-1. Lane の寸法と座標

```
Lane 幅: 110px
Lane 全高: 300px（内訳は下記）

  ┌──────────────────┐  ←  y=0
  │   sandy beach     │
  │  （ワニ可視エリア）│     height: 200px
  │                   │
  ├───────────────────┤  ← y=200（BushSvg 上端）
  │    BushSvg         │     height: 70px, z-index: 2
  └───────────────────┘  ← y=270（BushSvg 下端 / gator 基準点）
  [ A ]  キーラベル       height: 46px（margin-top: 8px）
  Total: ~324px
```

### 5-2. ワニ位置計算

```
.gator-container {
  position: absolute;
  bottom: 0;                   /* BushSvg の直上が bottom:0 */
  left: 50%;
  transform: translateX(-50%) translateY(${offsetY}px);
}
```

`offsetY` はレンダリング時に計算:

```ts
const riseRatio = GATOR_CONFIG[gator.kind].riseRatio;
const offsetY   = GATOR_HEIGHT * (1 - gator.riseProgress * riseRatio);
// offsetY=150: 完全に隠れている（BushSvg の裏）
// offsetY=0:   全身が BushSvg の上に出ている
// peek(riseRatio=0.35), riseProgress=1: offsetY = 150*(1-0.35) = 97.5px
//   → 52.5px が見える（頭の先のみ）
```

> BushSvg は z-index:2 のため、ワニが完全に上がっても tail は BushSvg に隠れる。  
> ワニの snout（SVG の上部）が最初に visible area に入る。

### 5-3. overflow 制御

`.lane-arena`（height:200px, `overflow: hidden`）でワニが下から出てくる前の状態を隠す。  
`.lane-bush`（`position: absolute; bottom: 0; z-index: 2`）でワニの下部・穴を隠す。

---

## 6. GatorSvg.tsx — SVG パス仕様

### 6-1. 基本構造

```
viewBox="0 0 70 150"
全高 150px 中の各パーツ配分:
  snout（吻）先端 ........  y = 2
  snout 基部（目の上） ..  y = 45
  目（中心） ...........  y = 58  cx=22,47
  頭部（最大幅 ~50px） .  y = 40〜75
  前脚付け根 ...........  y = 88
  体最大幅（~46px） ....  y = 80〜100
  後脚付け根 ...........  y = 118
  尾先端 ...............  y = 148
```

### 6-2. メインボディパス（`<path id="body">`）

```svg
<path d="
  M 35 2
  C 39 2, 47 10, 48 28
  C 49 38, 52 45, 54 58
  C 57 70, 58 82, 57 96
  C 56 110, 51 124, 44 138
  C 41 143, 38 148, 35 148
  C 32 148, 29 143, 26 138
  C 19 124, 14 110, 13 96
  C 12 82, 13 70, 16 58
  C 18 45, 21 38, 22 28
  C 23 10, 31 2, 35 2
  Z
"
  fill="#4a7c3f" stroke="#2d5016" stroke-width="1.5"
/>
```

### 6-3. 腹部ハイライト（`<ellipse id="belly">`）

```svg
<ellipse cx="35" cy="90" rx="14" ry="28"
  fill="#5a9c4e" opacity="0.5"/>
```

### 6-4. スケール模様（`<g id="scales">`）

```svg
<!-- 3本の弧で鱗感を出す -->
<path d="M 24 72 Q 35 69 46 72" stroke="#2d5016" stroke-width="1" fill="none" opacity="0.6"/>
<path d="M 22 90 Q 35 87 48 90" stroke="#2d5016" stroke-width="1" fill="none" opacity="0.6"/>
<path d="M 24 108 Q 35 105 46 108" stroke="#2d5016" stroke-width="1" fill="none" opacity="0.6"/>
```

### 6-5. 前脚（`<g id="front-legs">`）

```svg
<!-- 左前脚 -->
<path d="M 15 85 C 7 83, 3 88, 5 94 C 7 98, 11 97, 14 95"
  fill="#4a7c3f" stroke="#2d5016" stroke-width="1.2"/>
<!-- 右前脚 -->
<path d="M 55 85 C 63 83, 67 88, 65 94 C 63 98, 59 97, 56 95"
  fill="#4a7c3f" stroke="#2d5016" stroke-width="1.2"/>
```

### 6-6. 後脚（`<g id="back-legs">`）

```svg
<!-- 左後脚 -->
<path d="M 16 114 C 8 112, 4 117, 6 122 C 8 126, 12 125, 15 122"
  fill="#4a7c3f" stroke="#2d5016" stroke-width="1.2"/>
<!-- 右後脚 -->
<path d="M 54 114 C 62 112, 66 117, 64 122 C 62 126, 58 125, 55 122"
  fill="#4a7c3f" stroke="#2d5016" stroke-width="1.2"/>
```

### 6-7. 目（`<g id="eyes">`）

```svg
<!-- 左目 -->
<circle cx="22" cy="58" r="5.5" fill="#f0c030" stroke="#2d5016" stroke-width="1"/>
<circle cx="22" cy="58" r="2.5"  fill="#1a1a1a"/>
<circle cx="21" cy="57" r="0.8"  fill="#ffffff"/>  <!-- ハイライト -->
<!-- 右目 -->
<circle cx="48" cy="58" r="5.5" fill="#f0c030" stroke="#2d5016" stroke-width="1"/>
<circle cx="48" cy="58" r="2.5"  fill="#1a1a1a"/>
<circle cx="47" cy="57" r="0.8"  fill="#ffffff"/>
```

### 6-8. 口（`<g id="mouth">`）

**通常時（口閉じ）:**
```svg
<path d="M 24 42 L 46 42" stroke="#2d5016" stroke-width="1.5" stroke-linecap="round"/>
```

**biting 時（CSS で `.lower-jaw` グループに animation を適用）:**

```svg
<!-- 上顎（固定） -->
<g id="upper-jaw">
  <path d="M 35 2 … M 24 42" .../>   <!-- snout 上半分 -->
</g>
<!-- 下顎（噛み時に下にずれる） -->
<g id="lower-jaw" class="lower-jaw">
  <path d="M 24 42 C 28 48, 42 48, 46 42"
    fill="#c0392b" stroke="#2d5016" stroke-width="1"/>
  <!-- 歯（上下各3本） -->
  <path d="M 27 42 L 28 38 M 33 42 L 34 38 M 39 42 L 40 38 M 45 42 L 44 38"
    stroke="#f5f5f0" stroke-width="1.5" stroke-linecap="round"/>
  <path d="M 27 42 L 28 46 M 33 42 L 34 46 M 39 42 L 40 46 M 45 42 L 44 46"
    stroke="#f5f5f0" stroke-width="1.5" stroke-linecap="round"/>
</g>
```

> **注意**: 実装では通常時と biting 時で 2 パターンの SVG を切り替えるか、  
> CSS transform で `.lower-jaw` に `translateY` をかける。  
> **推奨**: state prop を受け取り、通常/噛み の 2 描画パターンをコンポーネント内で切り替える。

### 6-9. GatorSvg のプロップス

```ts
type GatorSvgProps = {
  kind: GatorKind;
  gatorPhase: GatorPhase;
  biteElapsed: number; // biting 時の経過 ms（口開き量の計算に使用）
};
```

---

## 7. BushSvg.tsx — 茂みSVG

```
viewBox="0 0 110 70"
幅 110px × 高さ 70px

3つの葉の塊（ランダムな形のブロブ）を重ねる:
  - 奥の葉（中央）:  淡緑 #3a8c28, 幅 80px, y=5〜55
  - 左の葉:          濃緑 #2d7a1f, 幅 55px, y=15〜65
  - 右の葉:          濃緑 #2d7a1f, 幅 55px, y=15〜65
  - 前面の葉（中央）: 中緑 #4aab35, 幅 60px, y=20〜70
```

```svg
<svg viewBox="0 0 110 70">
  <!-- 奥の葉：中央 -->
  <ellipse cx="55" cy="32" rx="42" ry="26" fill="#3a8c28"/>
  <!-- 左の葉 -->
  <ellipse cx="28" cy="42" rx="28" ry="22" fill="#2d7a1f"/>
  <!-- 右の葉 -->
  <ellipse cx="82" cy="42" rx="28" ry="22" fill="#2d7a1f"/>
  <!-- 前面の葉（中央、ツヤあり） -->
  <ellipse cx="55" cy="50" rx="30" ry="20" fill="#4aab35"/>
  <!-- ハイライト -->
  <ellipse cx="42" cy="44" rx="8"  ry="5" fill="#6ac94e" opacity="0.6"/>
  <ellipse cx="68" cy="44" rx="8"  ry="5" fill="#6ac94e" opacity="0.6"/>
</svg>
```

---

## 8. CSS アニメーション仕様（GatorSvg.css）

### 8-1. 叩かれフラッシュ（whacked）

```css
@keyframes gator-whacked {
  0%   { filter: brightness(1) saturate(1); }
  20%  { filter: brightness(2) saturate(0) sepia(1) hue-rotate(-30deg); }
  60%  { filter: brightness(1.5) saturate(0) sepia(1) hue-rotate(-30deg); }
  100% { filter: brightness(1) saturate(1); opacity: 0; }
}

.gator-svg.whacked {
  animation: gator-whacked 0.4s ease-out forwards;
}
```

### 8-2. 「いて！」テキスト

```css
@keyframes ite-float {
  0%   { opacity: 1; transform: translateY(0) scale(1); }
  70%  { opacity: 1; transform: translateY(-24px) scale(1.1); }
  100% { opacity: 0; transform: translateY(-36px) scale(0.9); }
}

.hit-text {
  position: absolute;
  top: -10px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 1.1rem;
  font-weight: 900;
  color: #e03030;
  white-space: nowrap;
  pointer-events: none;
  animation: ite-float 0.6s ease-out forwards;
  z-index: 10;
}
```

### 8-3. 噛みアニメーション（lower-jaw）

```css
@keyframes jaw-open {
  0%   { transform: translateY(0); }
  25%  { transform: translateY(20px); }   /* 0.35s で開く */
  82%  { transform: translateY(20px); }   /* 0.8s 保持 */
  100% { transform: translateY(0); }      /* 0.25s で閉じる */
}

.lower-jaw.biting {
  animation: jaw-open 1.4s ease-in-out forwards;
  transform-origin: center 42px; /* 口の付け根を軸にする */
}
```

### 8-4. 「ガブ！」テキスト

```css
@keyframes gabu-pop {
  0%   { opacity: 0; transform: translateX(-50%) scale(0.6); }
  20%  { opacity: 1; transform: translateX(-50%) scale(1.2); }
  60%  { opacity: 1; transform: translateX(-50%) scale(1); }
  100% { opacity: 0; transform: translateX(-50%) scale(0.8); }
}

.bite-text {
  position: absolute;
  top: 20px;
  left: 50%;
  font-size: 1.3rem;
  font-weight: 900;
  color: #c0392b;
  white-space: nowrap;
  pointer-events: none;
  animation: gabu-pop 0.9s ease-out forwards;
  z-index: 10;
}
```

---

## 9. AngerBanner.css

```css
@keyframes anger-enter {
  0%   { opacity: 0; transform: translate(-50%, -50%) scale(0.7); }
  50%  { opacity: 1; transform: translate(-50%, -50%) scale(1.05); }
  100% { opacity: 1; transform: translate(-50%, -50%) scale(1); }
}

@keyframes banner-shake {
  0%, 100% { transform: translate(-50%, -50%) rotate(0deg); }
  20%       { transform: translate(-50%, -50%) rotate(-2deg); }
  40%       { transform: translate(-50%, -50%) rotate(2deg); }
  60%       { transform: translate(-50%, -50%) rotate(-1.5deg); }
  80%       { transform: translate(-50%, -50%) rotate(1.5deg); }
}

.anger-banner {
  position: fixed;
  top: 40%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: rgba(200, 40, 20, 0.88);
  color: #fff;
  font-size: clamp(1.6rem, 4vw, 2.4rem);
  font-weight: 900;
  padding: 0.6em 1.2em;
  border-radius: 16px;
  border: 3px solid #ff8866;
  box-shadow: 0 0 40px rgba(255, 60, 20, 0.7);
  z-index: 100;
  animation:
    anger-enter 0.4s ease-out forwards,
    banner-shake 0.4s 0.4s ease-in-out;
  pointer-events: none;
}
```

---

## 10. コンポーネント詳細

### App.tsx

```tsx
export default function App() {
  const { gameState, startGame, whack } = useGame();
  const { gamePhase, score, biteCount, timeLeft, gators, angerBannerVisible } = gameState;

  return (
    <div className="app">
      <h1 className="title">わにわにパニック</h1>
      <HUD score={score} biteCount={biteCount} timeLeft={timeLeft} gamePhase={gamePhase} />
      <Board gators={gators} gamePhase={gamePhase} onWhack={whack} />
      {angerBannerVisible && <AngerBanner />}
      {gamePhase === "idle" && <StartOverlay onStart={startGame} />}
      {gamePhase === "gameover" && (
        <GameOverOverlay score={score} biteCount={biteCount} onRestart={startGame} />
      )}
    </div>
  );
}
```

### HUD.tsx

```tsx
type HUDProps = {
  score: number;
  biteCount: number;
  timeLeft: number;
  gamePhase: GamePhase;
};
// 表示: スコア: XX | 残り: XXs | 噛まれた: X
// timeLeft <= 10 かつ playing 系フェーズなら .danger クラスで赤く点滅
```

```css
@keyframes time-blink {
  0%, 100% { color: #c0392b; }
  50%       { color: #ff7c7c; }
}
.hud-time.danger { animation: time-blink 0.5s ease-in-out infinite; }
```

### Board.tsx

```tsx
type BoardProps = {
  gators: GatorState[];
  gamePhase: GamePhase;
  onWhack: (laneId: number) => void;
};

// laneId 0〜4 を map し、各 Lane に当該 laneId のワニを渡す
const KEYS = ["A", "S", "D", "F", "G"];
function gatorAt(gators: GatorState[], laneId: number): GatorState | undefined {
  return gators.find(g => g.laneId === laneId && g.gatorPhase !== "hidden");
}
```

### Lane.tsx

```tsx
type LaneProps = {
  laneId: number;
  keyLabel: string;    // "A"〜"G"
  gator?: GatorState;
  onWhack: (laneId: number) => void;
  isAnger: boolean;    // 怒りフェーズ中は背景を赤みがかった砂浜に
};
```

レンダリング構造（JSX）:
```tsx
<div className={`lane ${isAnger ? "lane--anger" : ""}`}>
  <div className="lane-arena" onClick={() => onWhack(laneId)}>
    {gator && gator.gatorPhase !== "hidden" && (
      <div
        className="gator-container"
        style={{
          transform: `translateX(-50%) translateY(${
            GATOR_HEIGHT * (1 - gator.riseProgress * GATOR_CONFIG[gator.kind].riseRatio)
          }px)`,
        }}
      >
        <GatorSvg
          kind={gator.kind}
          gatorPhase={gator.gatorPhase}
          biteElapsed={gator.biteElapsed}
        />
        {gator.gatorPhase === "whacked" && (
          <span className="hit-text">いて！</span>
        )}
        {gator.gatorPhase === "biting" && gator.biteElapsed < 500 && (
          <span className="bite-text">ガブ！</span>
        )}
      </div>
    )}
  </div>
  <div className="lane-bush">
    <BushSvg />
  </div>
  <div className="key-label">{keyLabel}</div>
</div>
```

### Lane.css（主要部分）

```css
.lane {
  width: 110px;
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  user-select: none;
}

.lane-arena {
  width: 110px;
  height: 200px;
  position: relative;
  overflow: hidden;
  cursor: pointer;
  background: linear-gradient(180deg, #f0ddb0 0%, #e8cb88 100%);
  border-radius: 8px 8px 0 0;
}

/* ワニクリック領域のホバー表示 */
.lane-arena:hover::after {
  content: "";
  position: absolute;
  inset: 0;
  background: rgba(255, 255, 255, 0.06);
  pointer-events: none;
}

.lane-bush {
  width: 110px;
  height: 70px;
  position: absolute;
  bottom: 46px; /* key-label の高さ分上 */
  left: 0;
  z-index: 2;
  pointer-events: none;
}

.gator-container {
  position: absolute;
  bottom: 0;
  left: 50%;
  /* transform は inline style で付与 */
  transition: none; /* rAF で滑らかにするため CSS transition は使わない */
  z-index: 1;
}

.key-label {
  margin-top: 6px;
  width: 38px;
  height: 38px;
  border: 2px solid #8b6914;
  border-radius: 7px;
  background: #f0d080;
  font-weight: 700;
  font-size: 1.05rem;
  text-align: center;
  line-height: 34px;
  color: #5a3a00;
  box-shadow: 0 3px 0 #8b6914;
  z-index: 3;
  transition: transform 80ms, box-shadow 80ms;
}

.key-label.pressed {
  transform: translateY(2px);
  box-shadow: 0 1px 0 #8b6914;
}
```

キーラベルの「pressed」表現は `keydown`/`keyup` イベントで CSS クラスをトグル。

---

## 11. index.css / App.css

```css
/* index.css */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html, body { height: 100%; }
body {
  font-family: "Hiragino Kaku Gothic ProN", "Noto Sans JP", sans-serif;
  background: #f5e6c8;
}

/* App.css */
.app {
  min-height: 100svh;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 24px 16px 32px;
  background: radial-gradient(ellipse at 50% 0%, #ffe9a0 0%, #f5e6c8 60%);
}

.title {
  font-size: clamp(1.6rem, 5vw, 2.4rem);
  font-weight: 900;
  color: #4a2a00;
  text-shadow: 2px 3px 0 #c8a050;
  margin-bottom: 16px;
  letter-spacing: 0.04em;
}

.board {
  display: flex;
  gap: 12px;
  padding: 20px 28px 12px;
  background: #e8ca80;
  border-radius: 20px;
  box-shadow: 0 4px 16px rgba(100, 60, 0, 0.25), inset 0 1px 0 rgba(255,255,255,0.4);
  margin-top: 16px;
}
```

---

## 12. フェーズ別視覚演出

| フェーズ | .board 背景 | .lane-arena 背景 | その他 |
|---------|------------|-----------------|--------|
| early | `#e8ca80` | 通常砂浜グラデ | — |
| mid | `#dfc070` | 砂浜 | — |
| late | `#d4a840` | やや暗い砂浜 | — |
| anger | `#c84020` + shake アニメ | 赤み砂浜 | AngerBanner 表示 |
| gameover | — | — | GameOverOverlay |

```css
@keyframes board-shake {
  0%, 100% { transform: translateX(0); }
  20%       { transform: translateX(-4px); }
  40%       { transform: translateX(4px); }
  60%       { transform: translateX(-3px); }
  80%       { transform: translateX(3px); }
}
.board.anger {
  background: #c84020;
  animation: board-shake 0.3s ease-in-out 3;
}
```

---

## 13. オーバーレイ共通 CSS（Overlay.css）

```css
.overlay-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(30, 15, 0, 0.72);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 16px;
  z-index: 50;
}

.overlay-title {
  font-size: clamp(1.6rem, 4vw, 2.2rem);
  font-weight: 900;
  color: #fff8e0;
  text-shadow: 0 2px 8px rgba(0,0,0,0.5);
}

.overlay-sub {
  font-size: 1rem;
  color: #ffe080;
}

.btn-start {
  padding: 0.7em 2em;
  font-size: 1.2rem;
  font-weight: 700;
  background: #f0c030;
  color: #3a2000;
  border: none;
  border-radius: 12px;
  cursor: pointer;
  box-shadow: 0 4px 0 #a07010;
  transition: transform 80ms, box-shadow 80ms;
}
.btn-start:hover  { background: #f8d050; }
.btn-start:active {
  transform: translateY(3px);
  box-shadow: 0 1px 0 #a07010;
}
```

StartOverlay はゲーム開始前に `keydown: Enter` でも `startGame()` を呼ぶ。

---

## 14. 未実装・将来対応（スコープ外）

- サウンド（叩いた音、噛まれた音、BGM、怒りボイス）
- モバイルタップ操作
- スコアランキング / ローカルストレージ保存
- アクセシビリティ（aria-live でスコア読み上げ等）
