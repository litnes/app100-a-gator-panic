# わにわにパニック 詳細設計書

## 1. ファイル構成

```
src/
├── main.tsx
├── index.css          # グローバルリセット・背景
├── App.tsx            # ルートコンポーネント
├── App.css
├── Lane.tsx           # 1列分（茂み＋ワニ）
├── Lane.css
├── GatorSvg.tsx       # ワニのSVG描画（種別・状態で表示変化）
├── AngerBanner.tsx    # 「もう、怒ったぞー！」表示
├── AngerBanner.css
├── useGame.ts         # ゲームロジック全体
├── usePhase.ts        # フェーズ管理
├── types.ts           # 型定義
└── constants.ts       # 定数
```

---

## 2. 型定義（types.ts）

```ts
export type GatorKind = "slow-full" | "quick-full" | "peek" | "biter";

export type GatorState = {
  laneId: number;          // 0〜4（列番号）
  kind: GatorKind;
  phase: "hidden" | "rising" | "up" | "biting" | "retreating" | "whacked";
  riseProgress: number;    // 0.0〜1.0（露出量）
};

export type GamePhase = "idle" | "early" | "mid" | "late" | "anger" | "gameover";

export type GameState = {
  phase: GamePhase;
  timeLeft: number;        // 残り秒数
  score: number;
  biteCount: number;
  gators: GatorState[];    // 現在アクティブなワニ（最大5体）
  angerActive: boolean;    // 怒りフェーズフラグ
};
```

---

## 3. 定数（constants.ts）

```ts
export const LANE_COUNT = 5;
export const GAME_DURATION = 60;        // 秒
export const ANGER_START_AT = 55;       // 残り時間がこれ以下で怒りフェーズ

// フェーズ切り替え（経過秒数）
export const PHASE_EARLY_END  = 20;
export const PHASE_MID_END    = 40;
export const PHASE_LATE_END   = 55;

// キーマップ（列インデックス）
export const KEY_MAP: Record<string, number> = {
  a: 0, s: 1, d: 2, f: 3, g: 4,
};

// ワニ種別ごとの設定
export const GATOR_CONFIG = {
  "slow-full":  { riseRatio: 1.0, duration: [1500, 2500], bites: false },
  "quick-full": { riseRatio: 1.0, duration: [500,   900], bites: false },
  "peek":       { riseRatio: 0.35, duration: [800,  1200], bites: false },
  "biter":      { riseRatio: 1.0, duration: [2000, 3000], bites: true  },
} as const;

// フェーズごとの出現ワニ種別確率テーブル（0〜1 の確率）
export const SPAWN_TABLE: Record<GamePhase, Partial<Record<GatorKind, number>>> = {
  idle:     {},
  early:    { "slow-full": 1.0 },
  mid:      { "slow-full": 0.4, "quick-full": 0.3, "peek": 0.2, "biter": 0.1 },
  late:     { "slow-full": 0.2, "quick-full": 0.3, "peek": 0.2, "biter": 0.3 },
  anger:    { "quick-full": 0.5, "biter": 0.5 },
  gameover: {},
};

// フェーズごとの同時出現最大数
export const MAX_CONCURRENT: Record<GamePhase, number> = {
  idle: 0, early: 1, mid: 3, late: 4, anger: 5, gameover: 0,
};

// フェーズごとの次スポーン待ち時間範囲[ms]
export const SPAWN_INTERVAL: Record<GamePhase, [number, number]> = {
  idle:     [0,    0   ],
  early:    [800,  1400],
  mid:      [500,  1000],
  late:     [300,  700 ],
  anger:    [100,  300 ],
  gameover: [0,    0   ],
};
```

---

## 4. ゲームロジック（useGame.ts）

### 4-1. 状態

```ts
const [gameState, setGameState] = useState<GameState>(initialState);
const schedulerRef = useRef<ReturnType<typeof setTimeout>[]>([]);
const countdownRef = useRef<ReturnType<typeof setInterval> | null>(null);
```

### 4-2. フェーズ管理（usePhase.ts に切り出し）

```ts
function calcPhase(elapsed: number, angerActive: boolean): GamePhase {
  if (angerActive) return "anger";
  if (elapsed < PHASE_EARLY_END) return "early";
  if (elapsed < PHASE_MID_END)   return "mid";
  return "late";
}
```

`timeLeft` が `ANGER_START_AT` 以下になったら怒りフェーズ突入シーケンス開始：
1. 全ワニを `retreating` → `hidden` にする（即時）
2. `angerActive = true` をセット
3. `AngerBanner` を表示（2秒）
4. バナー終了後、5列すべてから高速スポーン開始

### 4-3. スポーンロジック

```
scheduleNextSpawn():
  1. 現在アクティブなワニ数を確認
  2. MAX_CONCURRENT[phase] より少なければスポーン可能
  3. 空き列からランダムに1列を選ぶ
  4. SPAWN_TABLE[phase] の確率でワニ種別を決定
  5. 新しい GatorState を追加（phase: "rising"）
  6. SPAWN_INTERVAL[phase] のランダム ms 後に再帰呼び出し
```

### 4-4. ワニの出没アニメーション管理

各ワニは `useEffect` 内で独自タイマーを持つのではなく、
`useGame` 内で `requestAnimationFrame` ループを回して
`riseProgress` を更新する方式を採用する。

```
rAF ループ（毎フレーム）:
  各アクティブワニに対して:
    "rising"     → riseProgress += dt / riseDuration、1.0 で "up" に遷移
    "up"         → upTimer 経過後に "retreating"（biter は "biting" へ）
    "biting"     → biteAnimation 終了後 biteCount++、"retreating" へ
    "retreating" → riseProgress -= dt / retreatDuration、0 で "hidden" へ
    "whacked"    → 短い hitFlash 後 "hidden" へ
```

### 4-5. whack（叩き処理）

```ts
function whack(laneId: number) {
  // 対象列にワニが "rising" | "up" | "biting" 状態なら
  //   phase = "whacked" に変更
  //   score += (anger ? 2 : 1)
}
```

キー操作は `useEffect` で `keydown` イベントを登録し、`KEY_MAP[e.key.toLowerCase()]` で `laneId` を解決して `whack` を呼ぶ。

---

## 5. コンポーネント設計

### App.tsx

```tsx
<div class="app">
  <h1>わにわにパニック</h1>
  <HUD score biteCount timeLeft phase />
  <Board>
    {lanes.map(i => <Lane key={i} laneId={i} gator={gatorAt(i)} onWhack={whack} />)}
  </Board>
  {angerActive && <AngerBanner />}
  {phase === "idle" && <StartOverlay onStart={startGame} />}
  {phase === "gameover" && <GameOverOverlay score biteCount onRestart={startGame} />}
</div>
```

### Lane.tsx

```
Lane（列）構造:
  ┌──────────┐
  │  茂みSVG  │  ← position: absolute, z-index: 2
  │          │
  │ [GatorSvg] ← position: absolute, bottom から riseProgress % で位置制御
  │          │
  │ 砂浜部分  │
  └──────────┘
  [ A ] キー表示ラベル
```

- `overflow: hidden` で茂みより下のワニだけ見える
- `riseProgress` に応じて `translateY` を制御（0 = 完全に隠れている、1 = 全身表示）
- `peek` 種は `riseRatio: 0.35` で最大露出 35% で止まる

### GatorSvg.tsx

真上から見た SVG ワニ。状態によって変化：

| 状態 | 見た目 |
|------|--------|
| 通常 | 緑の全身・口閉じ |
| `biting` | 口が開くアニメーション（CSS animation） |
| `whacked` | 赤フラッシュ＋「いて！」テキスト |
| `peek` | 同じ SVG を上から一部だけ見せる（Lane の overflow で制御） |

#### SVG の構造（真上俯瞰）

```
       ┌──┐        ← 口先（吻）
      /    \
     │ ●  ● │      ← 目（左右）
      \    /
  ----│    │----   ← 前脚（左右に短く突き出る）
      │    │
      │    │
  ----│    │----   ← 後脚（左右に短く突き出る）
      │    │
       \  /
        \/         ← 尻尾
```

- 吻（スナウト）が縦長に長い
- 体幅に対して頭部が占める比率が大きい
- 全体サイズ：幅 70px × 高さ 140px 程度（CSS でスケール調整）
- カラーパレット：
  - 体上面：`#4a7c3f`
  - 体縁・模様：`#2d5016`
  - 目：`#ffcc00`
  - 口内：`#c0392b`
  - 腹（口を開いた時）：`#e8d5b7`

### AngerBanner.tsx

```
画面中央に大きなテキストでフェードイン:
  「もう、怒ったぞー！」
  背景：半透明の赤 + shake アニメーション
  表示時間：約 2 秒
```

---

## 6. CSS・ビジュアル仕様

### 背景・全体

```css
.app {
  background: #f5e6c8;           /* ライトベージュ砂浜 */
  min-height: 100svh;
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### 茂みのビジュアル

SVG または CSS で描く南国風の茂み：
- 濃い緑（`#2d7a1f`）の葉が重なったシルエット
- 幅 100px、高さ 60px 程度
- 各列の上部に配置

### レーン

```css
.lane {
  width: 100px;
  height: 220px;          /* 茂みエリア + 砂浜エリア */
  position: relative;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.lane-bush {
  /* 茂み：上部 60px */
  position: absolute;
  top: 0;
  z-index: 2;
}

.lane-gator {
  position: absolute;
  bottom: 0;              /* 砂浜の底から出てくる */
  transform: translateY(calc((1 - riseProgress) * 140px));
  z-index: 1;
}
```

### ボード

```css
.board {
  display: flex;
  gap: 16px;
  flex-direction: row;
  padding: 24px;
  background: /* 砂浜テクスチャ or グラデーション */;
  border-radius: 16px;
}
```

### キーラベル

```css
.key-label {
  margin-top: 8px;
  width: 36px;
  height: 36px;
  border: 2px solid #8b6914;
  border-radius: 6px;
  background: #f0d080;
  font-weight: 700;
  font-size: 1.1rem;
  text-align: center;
  line-height: 32px;
  color: #5a3a00;
  box-shadow: 0 3px 0 #8b6914;  /* キーボード風立体感 */
}

.key-label.active {
  transform: translateY(2px);
  box-shadow: 0 1px 0 #8b6914;
}
```

---

## 7. アニメーション仕様

| アニメーション | 実装方法 | 詳細 |
|--------------|----------|------|
| ワニ出現 | `rAF` ループで `translateY` | `easeOut` 相当の補間 |
| ワニ引っ込み | `rAF` ループで `translateY` | `easeIn` 相当 |
| 叩かれた | CSS `@keyframes` | 赤フラッシュ 0.3s |
| 「いて！」 | CSS `@keyframes` | 上にふわっと浮いて消える |
| 口開け（噛み） | CSS `@keyframes` | 0.3s で開いて 0.3s 保持 |
| 「ガブッ！」 | CSS `@keyframes` | 赤文字で出現・消滅 |
| 怒りバナー | CSS `@keyframes` | フェードイン + shake |
| キーラベル押下 | CSS transition | translateY +2px |

---

## 8. 今後の検討事項

- [ ] ワニ SVG の最終デザイン確定（現在はスケッチ段階）
- [ ] 茂みのビジュアル実装方法（SVG vs 画像 vs pure CSS）
- [ ] 怒りフェーズの詳細挙動（何秒間？終了条件？）
- [ ] 噛みアニメーションの具体的な口の開き量・タイミング
- [ ] モバイル対応（タップ操作）
- [ ] スコア演出（コンボ表示など）
- [ ] サウンド実装方針
- [ ] テスト方針（ゲームロジックのユニットテスト）
